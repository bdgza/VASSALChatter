<?xml version="1.0" encoding="UTF-8"?>
<ContentPage
	xmlns="http://xamarin.com/schemas/2014/forms"
	xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
	xmlns:local="clr-namespace:VASSALChatter;assembly=VASSALChatter"
	x:Class="VASSALChatter.ChatPage"
	Title="Chat"
	Icon="chat.png">
	<ContentPage.Content>
		<StackLayout>

			<local:NativeChatListView x:Name="listView"
				
		        ItemsSource="{Binding Messages}"
		        HasUnevenRows="true"
		        IsPullToRefreshEnabled="false"
				SeparatorVisibility="None"
				Focused="ListView_Focused"
				Grid.Row="0"
				ItemTapped="ListView_ItemTapped"
				Margin="0,0,0,0">
		      <local:NativeChatListView.ItemTemplate>
		        <DataTemplate>
		          <ViewCell>
					<Label Text="{Binding Value}" FontSize="14" LineBreakMode="WordWrap" TextColor="{Binding Color}">
						<Label.FontFamily>
					        <OnPlatform x:TypeArguments="x:String">
					            <OnPlatform.iOS>Helvetica</OnPlatform.iOS>
					            <OnPlatform.Android></OnPlatform.Android>
					            <OnPlatform.WinPhone></OnPlatform.WinPhone>
					        </OnPlatform>
					    </Label.FontFamily>
					</Label>
		          </ViewCell>
		        </DataTemplate>
		      </local:NativeChatListView.ItemTemplate>
		    </local:NativeChatListView>
			
		</StackLayout>
	</ContentPage.Content>
</ContentPage>
